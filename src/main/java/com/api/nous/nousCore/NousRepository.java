package com.api.nous.nousCore;

import org.springframework.data.jpa.repository.JpaRepository;

public interface NousRepository extends JpaRepository<Nous, Integer> {

    //This is the main repo for nous;
}
